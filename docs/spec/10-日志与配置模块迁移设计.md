# SPEC-10: 日志模块与配置模块迁移设计

> 定义 `ncatbot/utils/` 下日志模块和配置模块的新设计，替换 `ncatbot_legacy/utils/` 中的对应实现。

## 0. 设计原则

| 原则 | 说明 |
|------|------|
| **不向前兼容** | 完全重写，不保留 legacy 接口签名 |
| **关注点分离** | 配置模块拆分为 数据模型 / 存储 / 管理器 三层 |
| **结构化日志** | 对外保持 `get_log(name)` 接口，内部基于 `bind` 实现上下文传递 |
| **零全局副作用** | 模块导入不再自动执行 `setup_logging()`，由应用入口显式调用 |
| **最小依赖** | 日志模块仅依赖标准库 + Color 资源；配置模块仅依赖 pydantic + pyyaml |

---

## 1. 目录结构

```
ncatbot/utils/
├── __init__.py              # 包导出
├── logger/
│   ├── __init__.py          # 导出 get_log, setup_logging, tqdm
│   ├── core.py              # BoundLogger 包装类、get_log()
│   ├── formatter.py         # ColoredFormatter、FileFormatter
│   ├── filters.py           # MessageFoldFilter
│   ├── setup.py             # setup_logging() 全局初始化
│   └── tqdm.py              # 自定义 tqdm 进度条
├── config/
│   ├── __init__.py          # 导出 ConfigManager, get_config_manager, 模型
│   ├── models.py            # 纯 Pydantic 数据模型（Config / NapCatConfig / PluginConfig）
│   ├── storage.py           # ConfigStorage YAML 读写
│   ├── manager.py           # ConfigManager 管理器
│   └── security.py          # 密码校验 / token 生成
```

---

## 2. 日志模块设计

### 2.1 核心思路

legacy 的 `FoldedLogger` 继承 `logging.Logger`，在每个日志方法中重复覆写 `debug/info/warning/error` 做消息截断，并且 `stacklevel` 手动管理。新设计采用 **包装器模式**：

- `BoundLogger` 不继承 `logging.Logger`，而是 **持有** 一个标准 `logging.Logger` 实例。
- 通过 `bind(**ctx)` 返回新的 `BoundLogger`，上下文键值对会自动注入到每条日志的 `extra` 中。
- 消息截断 / base64 折叠 作为 `logging.Filter` 实现，从 Logger 类中解耦。

#### 2.1.1 关于 DEBUG 过滤 — 移除"注册 logger"机制

legacy 设计通过 `Status._registered_loggers` 集合 + `RegisteredLoggerFilter` 实现"只在控制台显示应用自身的 DEBUG 日志，屏蔽第三方库 DEBUG 噪声"。初版迁移设计将此机制平移为模块级 `_registered_loggers` 集合 + `is_registered_logger()` / `get_registered_loggers()` 函数，但存在以下问题：

| 问题 | 说明 |
|------|------|
| **全局可变状态未消除** | `_registered_loggers: set` 是模块级裸集合，无线程安全保护，本质上与 legacy `Status` 的全局可变状态一样，只是换了寄存位置 |
| **包内反向依赖** | `filters.py` 需要 `from core import is_registered_logger`，形成 filters → core 的反向依赖，而 core 是包的核心入口 |
| **对标准 logging 的 hack** | 注册表 + 自定义 Filter 是对标准级别管理机制的重复造轮子 |
| **公开 API 无实际消费者** | `is_registered_logger` / `get_registered_loggers` 对外部调用者没有意义，只服务于 Filter 内部判断 |

**替代方案：用标准 logging 级别管理**

```
root logger  ─── level: INFO         ← 第三方库继承此级别，自动屏蔽 DEBUG
  ├── console handler ── level: DEBUG ← handler 本身不限制
  └── file handler ───── level: DEBUG

get_log("my.app") ── level: DEBUG     ← 应用 logger 显式设为 DEBUG
```

- `setup_logging()` 将 root logger 设为 `INFO`
- `get_log(name)` 将该 logger 显式 `setLevel(DEBUG)`
- 第三方库 logger 继承 root 的 `INFO`，其 DEBUG 自然不输出
- **不需要 Filter，不需要注册表，不需要全局可变状态，filters.py 零包内依赖**

### 2.2 模块详细设计

#### 2.2.1 `core.py` — BoundLogger 与 get_log

```python
"""日志核心：BoundLogger 包装器和 get_log 工厂函数。"""

import logging
from typing import Any, Optional


class BoundLogger:
    """包装标准 Logger，支持 bind 上下文和消息预处理。

    用法:
        log = get_log("plugin.my_plugin")
        log.info("启动完成")

        # 绑定上下文
        log = log.bind(user_id="12345", group_id="67890")
        log.info("收到消息")  # extra 中自动携带 user_id, group_id
    """

    __slots__ = ("_logger", "_context")

    def __init__(self, logger: logging.Logger, context: dict[str, Any] | None = None):
        self._logger = logger
        self._context = context or {}

    # ---- 核心 API ----

    def bind(self, **kwargs: Any) -> "BoundLogger":
        """返回携带新上下文的 BoundLogger（不可变，返回新实例）。"""
        merged = {**self._context, **kwargs}
        return BoundLogger(self._logger, merged)

    def unbind(self, *keys: str) -> "BoundLogger":
        """移除指定上下文键，返回新实例。"""
        new_ctx = {k: v for k, v in self._context.items() if k not in keys}
        return BoundLogger(self._logger, new_ctx)

    # ---- 日志方法 ----

    def debug(self, msg: str, *args, **kwargs):
        self._log(logging.DEBUG, msg, args, kwargs)

    def info(self, msg: str, *args, **kwargs):
        self._log(logging.INFO, msg, args, kwargs)

    def warning(self, msg: str, *args, **kwargs):
        self._log(logging.WARNING, msg, args, kwargs)

    def error(self, msg: str, *args, **kwargs):
        self._log(logging.ERROR, msg, args, kwargs)

    def critical(self, msg: str, *args, **kwargs):
        self._log(logging.CRITICAL, msg, args, kwargs)

    def exception(self, msg: str, *args, **kwargs):
        kwargs.setdefault("exc_info", True)
        self._log(logging.ERROR, msg, args, kwargs)

    # ---- 属性代理 ----

    @property
    def name(self) -> str:
        return self._logger.name

    @property
    def level(self) -> int:
        return self._logger.level

    def setLevel(self, level: int | str) -> None:
        self._logger.setLevel(level)

    def isEnabledFor(self, level: int) -> bool:
        return self._logger.isEnabledFor(level)

    # ---- 内部实现 ----

    def _log(self, level: int, msg: str, args: tuple, kwargs: dict):
        if not self._logger.isEnabledFor(level):
            return
        # 合并 bind 上下文到 extra
        extra = kwargs.pop("extra", {})
        extra.update(self._context)
        kwargs["extra"] = extra
        kwargs.setdefault("stacklevel", 2)
        self._logger.log(level, msg, *args, **kwargs)


def get_log(name: Optional[str] = None) -> BoundLogger:
    """获取 BoundLogger 实例。

    通过 get_log() 创建的 logger 会被显式设为 DEBUG 级别，
    而第三方库的 logger 继承 root 的 INFO 级别，其 DEBUG 自然不输出。
    无需注册表或自定义 Filter 来控制 DEBUG 可见性。

    Args:
        name: logger 名称，None 返回 root logger 的包装。

    Returns:
        BoundLogger 实例。
    """
    logger = logging.getLogger(name)
    if name is not None:
        # 应用 logger 显式设为 DEBUG，使其 DEBUG 日志能通过
        # 第三方库 logger 继承 root(INFO)，自动屏蔽 DEBUG
        logger.setLevel(logging.DEBUG)
    return BoundLogger(logger)
```

**与 legacy 的关键差异**：

| 对比项 | Legacy (`FoldedLogger`) | 新设计 (`BoundLogger`) |
|--------|------------------------|----------------------|
| 继承方式 | 继承 `logging.Logger` | 包装 `logging.Logger`（组合优于继承） |
| 上下文传递 | 无 | `bind(user_id=xxx)` 链式绑定 |
| 消息截断 | 在每个日志方法中重复 | 独立 `MessageFoldFilter` |
| stacklevel | 手动 `kwargs["stacklevel"] = 2` | 统一在 `_log` 中处理 |
| DEBUG 过滤 | `Status` 注册表 + `RegisteredLoggerFilter` | **标准级别管理**：`get_log` 设 DEBUG，root 设 INFO |

#### 2.2.2 `formatter.py` — 格式化器

```python
"""日志格式化器。"""

import logging
import warnings
from ncatbot.utils.assets.color import Color

# 各级别颜色映射
LEVEL_COLORS = {
    "DEBUG":    Color.CYAN,
    "INFO":     Color.GREEN,
    "WARNING":  Color.YELLOW,
    "ERROR":    Color.RED,
    "CRITICAL": Color.MAGENTA,
}

# 控制台格式模板（按级别区分）
CONSOLE_FORMATS = {
    "DEBUG":    f"{Color.CYAN}[%(asctime)s.%(msecs)03d]{Color.RESET} "
                f"{Color.BLUE}%(levelname)-8s{Color.RESET} "
                f"{Color.MAGENTA}%(name)s{Color.RESET} "
                f"{Color.YELLOW}'%(filename)s:%(lineno)d'{Color.RESET} "
                "| %(message)s",
    "INFO":     f"{Color.CYAN}[%(asctime)s.%(msecs)03d]{Color.RESET} "
                f"{Color.GREEN}%(levelname)-8s{Color.RESET} "
                f"{Color.MAGENTA}%(name)s{Color.RESET} "
                f"{Color.GRAY}'%(filename)s:%(lineno)d'{Color.RESET} ➜ "
                f"{Color.WHITE}%(message)s{Color.RESET}",
    "WARNING":  f"{Color.CYAN}[%(asctime)s.%(msecs)03d]{Color.RESET} "
                f"{Color.YELLOW}%(levelname)-8s{Color.RESET} "
                f"{Color.MAGENTA}%(name)s{Color.RESET} "
                f"{Color.GRAY}'%(filename)s:%(lineno)d'{Color.RESET} "
                f"{Color.RED}➜{Color.RESET} "
                f"{Color.YELLOW}%(message)s{Color.RESET}",
    "ERROR":    f"{Color.CYAN}[%(asctime)s.%(msecs)03d]{Color.RESET} "
                f"{Color.RED}%(levelname)-8s{Color.RESET} "
                f"{Color.GRAY}'%(filename)s:%(lineno)d'{Color.RESET}"
                f"{Color.MAGENTA} %(name)s{Color.RESET} "
                f"{Color.RED}➜{Color.RESET} "
                f"{Color.RED}%(message)s{Color.RESET}",
    "CRITICAL": f"{Color.CYAN}[%(asctime)s.%(msecs)03d]{Color.RESET} "
                f"{Color.BG_RED}{Color.WHITE}%(levelname)-8s{Color.RESET} "
                f"{Color.GRAY}{{%(module)s}}{Color.RESET}"
                f"{Color.MAGENTA} '%(filename)s:%(lineno)d'{Color.RESET}"
                f"{Color.MAGENTA} %(name)s{Color.RESET} "
                f"{Color.BG_RED}➜{Color.RESET} "
                f"{Color.BOLD}%(message)s{Color.RESET}",
}

FILE_FORMAT = (
    "[%(asctime)s.%(msecs)03d] %(levelname)-8s %(name)s "
    "'%(filename)s:%(lineno)d' ➜ %(message)s"
)


class ColoredFormatter(logging.Formatter):
    """根据日志级别动态切换格式模板的控制台 Formatter。"""

    def __init__(self, datefmt: str = "%H:%M:%S"):
        super().__init__(datefmt=datefmt)
        self._formatters = {
            level: logging.Formatter(fmt, datefmt=datefmt)
            for level, fmt in CONSOLE_FORMATS.items()
        }
        self._default = logging.Formatter(CONSOLE_FORMATS["INFO"], datefmt=datefmt)

    def format(self, record: logging.LogRecord) -> str:
        formatter = self._formatters.get(record.levelname, self._default)
        return formatter.format(record)


class FileFormatter(logging.Formatter):
    """文件日志 Formatter，无颜色码。"""

    def __init__(self, datefmt: str = "%Y-%m-%d %H:%M:%S"):
        super().__init__(FILE_FORMAT, datefmt=datefmt)
```

**与 legacy 的关键差异**：

| 对比项 | Legacy (`ColoredFormatter`) | 新设计 |
|--------|---------------------------|--------|
| 格式切换 | 运行时拼接 `colored_levelname` 到单一模板 | 每个级别独立 Formatter 实例，按级别分发 |
| 异常处理 | try/except 返回 `[FORMAT ERROR]` | 不需要，每个 Formatter 独立 |
| 文件格式 | 与控制台格式混在 `default_log_format` dict | 独立 `FileFormatter` 类 |

#### 2.2.3 `filters.py` — 过滤器

移除 `RegisteredLoggerFilter`（DEBUG 过滤改由标准级别管理实现，见 2.1.1），仅保留 `MessageFoldFilter`。

```python
"""日志过滤器。"""

import re
import logging


class MessageFoldFilter(logging.Filter):
    """折叠超长消息和 base64 内容。

    - 替换连续 1000+ 字符的 base64 片段为 [BASE64_CONTENT]
    - 总长超过 2000 字符时截断
    """

    _BASE64_RE = re.compile(r"[A-Za-z0-9+/\\]{1000,}={0,2}")
    MAX_LENGTH = 2000

    def filter(self, record: logging.LogRecord) -> bool:
        msg = record.getMessage()
        if len(msg) > 1000:
            msg = self._BASE64_RE.sub("[BASE64_CONTENT]", msg)
        if len(msg) > self.MAX_LENGTH:
            msg = msg[:self.MAX_LENGTH] + "...[TRUNCATED]"
        record.msg = msg
        record.args = None  # 已格式化，清除 args
        return True
```

**与 legacy 的关键差异**：

| 对比项 | Legacy | 新设计 |
|--------|--------|--------|
| DEBUG 过滤 | `RegisteredLoggerFilter` + `Status` 注册表 | **移除**，改用标准级别管理（见 2.1.1） |
| 消息折叠 | 在 `FoldedLogger` 每个方法中重复调用 `self.reset()` | 独立 `MessageFoldFilter`，挂载到 Handler |
| base64 正则 | 每次调用时编译 | 预编译为类属性 |
| 包内依赖 | `filters.py` → `core.py`（反向依赖） | `filters.py` 零依赖 |

#### 2.2.4 `setup.py` — 全局初始化

```python
"""日志系统全局初始化。"""

import os
import re
import logging
from logging.handlers import TimedRotatingFileHandler
from typing import Sequence

from .formatter import ColoredFormatter, FileFormatter
from .filters import MessageFoldFilter

# 路由规则：logger name 正则 → 日志文件名
DEFAULT_ROUTING_RULES: list[tuple[str, str]] = [
    ("database", "db.log"),
    ("network",  "network.log"),
]

_initialized = False


def setup_logging(
    *,
    console_level: str | None = None,
    file_level: str | None = None,
    log_dir: str | None = None,
    backup_count: int | None = None,
    routing_rules: Sequence[tuple[str, str]] | None = None,
) -> None:
    """初始化全局日志系统。应在应用启动时调用一次。

    Args:
        console_level: 控制台日志级别，默认读 LOG_LEVEL 环境变量，兜底 DEBUG。
        file_level:    文件日志级别，默认读 FILE_LOG_LEVEL 环境变量，兜底 DEBUG。
        log_dir:       日志目录，默认读 LOG_FILE_PATH 环境变量，兜底 ./logs。
        backup_count:  日志保留天数，默认读 BACKUP_COUNT 环境变量，兜底 7。
        routing_rules: 路由规则列表，None 使用 DEFAULT_ROUTING_RULES。
    """
    global _initialized
    if _initialized:
        return
    _initialized = True

    console_level = (console_level or os.getenv("LOG_LEVEL", "DEBUG")).upper()
    file_level = (file_level or os.getenv("FILE_LOG_LEVEL", "DEBUG")).upper()
    log_dir = log_dir or os.getenv("LOG_FILE_PATH", "./logs")
    backup_count = backup_count or int(os.getenv("BACKUP_COUNT", "7"))
    rules = routing_rules if routing_rules is not None else DEFAULT_ROUTING_RULES

    os.makedirs(log_dir, exist_ok=True)

    # ---- root logger ----
    # root 设为 INFO：第三方库 logger 继承此级别，自动屏蔽 DEBUG
    # 应用 logger 通过 get_log() 显式设为 DEBUG（见 core.py）
    root = logging.getLogger()
    root.setLevel(logging.INFO)
    root.handlers.clear()

    # 控制台 handler — level 设为 DEBUG，允许应用 logger 的 DEBUG 通过
    console_handler = logging.StreamHandler()
    console_handler.setLevel(logging.DEBUG)
    console_handler.setFormatter(ColoredFormatter())
    console_handler.addFilter(MessageFoldFilter())
    root.addHandler(console_handler)

    # 主文件 handler
    main_fh = TimedRotatingFileHandler(
        os.path.join(log_dir, "bot.log"),
        when="midnight", interval=1,
        backupCount=backup_count, encoding="utf-8",
    )
    main_fh.suffix = "%Y_%m_%d"
    main_fh.setLevel(file_level)
    main_fh.setFormatter(FileFormatter())
    main_fh.addFilter(MessageFoldFilter())
    root.addHandler(main_fh)

    # ---- 路由规则 handler ----
    for pattern_str, filename in rules:
        try:
            compiled = re.compile(pattern_str)
        except re.error:
            continue

        fh = TimedRotatingFileHandler(
            os.path.join(log_dir, filename),
            when="midnight", interval=1,
            backupCount=backup_count, encoding="utf-8",
        )
        fh.suffix = "%Y_%m_%d"
        fh.setLevel(file_level)
        fh.setFormatter(FileFormatter())
        fh.addFilter(lambda record, p=compiled: bool(p.match(record.name)))
        root.addHandler(fh)
```

**与 legacy 的关键差异**：

| 对比项 | Legacy | 新设计 |
|--------|--------|--------|
| 初始化时机 | **模块导入时自动执行** `setup_logging()` | 显式调用，有 `_initialized` 幂等保护 |
| root level | `logging.INFO`（DEBUG 日志丢失） | `logging.INFO`（第三方不输出 DEBUG），应用 logger 由 `get_log()` 设为 DEBUG |
| DEBUG 过滤 | `RegisteredLoggerFilter` + 注册表 | 不需要 Filter，标准级别管理（见 2.1.1） |
| 参数化 | 仅环境变量 | 支持参数传入 + 环境变量兜底 |
| 格式 | 控制台/文件格式混在一个大 dict | 独立 Formatter 类 |

#### 2.2.5 `tqdm.py` — 进度条

从 legacy 平移，无需重设计。保留 `Color` 集成的自定义 `tqdm`。

#### 2.2.6 `logger/__init__.py` — 导出

```python
from .core import get_log, BoundLogger
from .setup import setup_logging
from .tqdm import tqdm

__all__ = [
    "get_log",
    "setup_logging",
    "tqdm",
    "BoundLogger",
]
```

### 2.3 使用示例

```python
from ncatbot.utils.logger import get_log, setup_logging

# 应用入口调用一次
setup_logging(console_level="INFO")

# 获取 logger
log = get_log("plugin.my_plugin")
log.info("插件加载完成")

# 绑定上下文
log = log.bind(user_id="12345", group_id="67890")
log.info("处理消息")            # extra 自动携带上下文
log.warning("消息处理超时")

# 解绑
log = log.unbind("group_id")
log.info("用户级操作")          # 只携带 user_id
```

### 2.4 日志模块依赖关系

```
logger/__init__.py
├── core.py          ← 仅依赖 logging 标准库
├── formatter.py     ← 依赖 assets.color
├── filters.py       ← 仅依赖 logging + re 标准库（零包内依赖）
├── setup.py         ← 依赖 formatter, filters
└── tqdm.py          ← 依赖 tqdm, assets.color
```

> 注意：`filters.py` 不再依赖 `core.py`，包内无反向依赖。

---

## 3. 配置模块设计

### 3.1 核心思路

legacy 配置模块结构基本合理（models / storage / manager 三层），迁移时做以下改进：

1. **数据模型层 (`models.py`)** — 纯数据定义，不包含任何业务逻辑（安全校验移到 `security.py`，`ensure_dir_exists` 移到 manager）。
2. **存储层 (`storage.py`)** — 无变化，保持 YAML 原子读写。
3. **管理器层 (`manager.py`)** — 精简属性代理，`update_value` 使用类型安全方式。
4. **安全工具 (`security.py`)** — 从 `utils.py` 更名，职责更清晰。

### 3.2 模块详细设计

#### 3.2.1 `models.py` — 纯数据模型

```python
"""配置数据模型 - 纯 Pydantic 结构定义，不含业务逻辑。"""

import os
from typing import Dict, List, Optional
from pydantic import BaseModel, ConfigDict, Field, field_validator


# ==================== 常量 ====================

DEFAULT_BOT_UIN = "123456"
DEFAULT_ROOT = "123456"


# ==================== 基础配置类 ====================

class BaseConfig(BaseModel):
    """配置基类。"""

    model_config = ConfigDict(validate_assignment=True, extra="allow")

    def get_field_paths(self, prefix: str = "") -> Dict[str, str]:
        """递归获取所有字段的点分路径映射。"""
        paths = {}
        for field_name in type(self).model_fields:
            current = f"{prefix}.{field_name}" if prefix else field_name
            paths[field_name] = current
            value = getattr(self, field_name)
            if isinstance(value, BaseConfig):
                paths.update(value.get_field_paths(current))
        return paths

    def to_dict(self) -> dict:
        return self.model_dump()


# ==================== 子配置模型 ====================

class PluginConfig(BaseConfig):
    """插件相关配置。"""

    plugins_dir: str = Field(default="plugins")
    plugin_whitelist: List[str] = Field(default_factory=list)
    plugin_blacklist: List[str] = Field(default_factory=list)
    load_plugin: bool = False

    @field_validator("plugins_dir")
    @classmethod
    def _validate_plugins_dir(cls, v: str) -> str:
        return v if v else "plugins"


class NapCatConfig(BaseConfig):
    """NapCat 客户端连接配置。"""

    ws_uri: str = "ws://localhost:3001"
    ws_token: str = "napcat_ws"
    ws_listen_ip: str = "localhost"
    webui_uri: str = "http://localhost:6099"
    webui_token: str = "napcat_webui"
    enable_webui: bool = True
    enable_update_check: bool = False
    stop_napcat: bool = False
    remote_mode: bool = False

    @field_validator("ws_uri")
    @classmethod
    def _validate_ws_uri(cls, v: str) -> str:
        if not v:
            return "ws://localhost:3001"
        if not (v.startswith("ws://") or v.startswith("wss://")):
            return f"ws://{v}"
        return v

    @field_validator("webui_uri")
    @classmethod
    def _validate_webui_uri(cls, v: str) -> str:
        if not v:
            return "http://localhost:6099"
        if not (v.startswith("http://") or v.startswith("https://")):
            return f"http://{v}"
        return v

    @property
    def ws_host(self) -> Optional[str]:
        import urllib.parse
        return urllib.parse.urlparse(self.ws_uri).hostname

    @property
    def ws_port(self) -> Optional[int]:
        import urllib.parse
        return urllib.parse.urlparse(self.ws_uri).port

    @property
    def webui_host(self) -> Optional[str]:
        import urllib.parse
        return urllib.parse.urlparse(self.webui_uri).hostname

    @property
    def webui_port(self) -> Optional[int]:
        import urllib.parse
        return urllib.parse.urlparse(self.webui_uri).port


class Config(BaseConfig):
    """主配置模型 — 聚合所有子配置。"""

    napcat: NapCatConfig = Field(default_factory=NapCatConfig)
    plugin: PluginConfig = Field(default_factory=PluginConfig)
    bot_uin: str = DEFAULT_BOT_UIN
    root: str = DEFAULT_ROOT
    debug: bool = False
    enable_webui_interaction: bool = True
    github_proxy: Optional[str] = Field(
        default_factory=lambda: os.getenv("GITHUB_PROXY")
    )
    check_ncatbot_update: bool = True
    skip_ncatbot_install_check: bool = False
    websocket_timeout: int = 15

    @field_validator("bot_uin", "root", mode="before")
    @classmethod
    def _coerce_to_str(cls, v) -> str:
        return str(v)

    @field_validator("websocket_timeout")
    @classmethod
    def _validate_timeout(cls, v: int) -> int:
        return max(1, v)

    def to_dict(self) -> dict:
        return self.model_dump(exclude_none=True)
```

**与 legacy `models.py` 的关键差异**：

| 对比项 | Legacy | 新设计 |
|--------|--------|--------|
| `NapCatConfig.get_issues()` | 在模型中做安全检查 + 自动修复 | **移除**，安全逻辑全部在 `manager.py` |
| `NapCatConfig.fix_security_issues()` | 模型直接修改自身 token | **移除**，由 manager 调用 `security.py` |
| `PluginConfig.ensure_dir_exists()` | 模型中创建目录 | **移除**，由 manager 负责 |
| `DEFAULT_WS_TOKEN` / `DEFAULT_WEBUI_TOKEN` | 常量定义在 models | **移除**，默认值直接写在字段定义 |
| `urllib.parse` import | 顶层导入 | 延迟导入（仅 property 使用） |
| `is_local()` / `is_default_uin()` | 在 `Config` 模型 | **移除**，由 manager 提供 |

#### 3.2.2 `storage.py` — 存储层

与 legacy 基本一致，无需重设计。保持原子写入。

```python
"""配置文件存储 — YAML 读写。"""

import os
import yaml
from typing import Any, Dict, Optional
from .models import Config

CONFIG_PATH = os.getenv(
    "NCATBOT_CONFIG_PATH",
    os.path.join(os.getcwd(), "config.yaml"),
)


class ConfigStorage:
    """配置文件的读写。"""

    def __init__(self, path: Optional[str] = None):
        self.path = path or CONFIG_PATH

    def load(self) -> Config:
        data = self._load_raw()
        return Config.model_validate(data)

    def save(self, config: Config) -> None:
        self._save_raw(config.to_dict())

    def exists(self) -> bool:
        return os.path.exists(self.path)

    def _load_raw(self) -> Dict[str, Any]:
        if not os.path.exists(self.path):
            return {}
        with open(self.path, "r", encoding="utf-8") as f:
            return yaml.safe_load(f) or {}

    def _save_raw(self, data: Dict[str, Any]) -> None:
        dir_path = os.path.dirname(self.path)
        if dir_path and not os.path.exists(dir_path):
            os.makedirs(dir_path)
        tmp_path = f"{self.path}.tmp"
        with open(tmp_path, "w", encoding="utf-8") as f:
            yaml.dump(data, f, allow_unicode=True, default_flow_style=False)
        os.replace(tmp_path, self.path)
```

#### 3.2.3 `security.py` — 安全工具

从 legacy `utils.py` 更名，逻辑不变。

```python
"""配置安全工具 — 密码校验与 token 生成。"""

import random
import string
from re import search

URI_SPECIAL_CHARS = "-_.~!()*"


def strong_password_check(password: str) -> bool:
    """检查密码强度：≥12 位，包含数字、大小写字母、特殊符号。"""
    patterns = [r"\d", "[a-z]", "[A-Z]"]
    return (
        len(password) >= 12
        and all(search(p, password) for p in patterns)
        and any(c in URI_SPECIAL_CHARS for c in password)
    )


def generate_strong_token(length: int = 16) -> str:
    """生成满足强度策略的随机 token。"""
    all_chars = string.ascii_letters + string.digits + URI_SPECIAL_CHARS
    while True:
        token = "".join(random.choice(all_chars) for _ in range(length))
        if strong_password_check(token):
            return token
```

#### 3.2.4 `manager.py` — 配置管理器

```python
"""配置管理器 — 对外暴露的主要接口。"""

import os
from typing import List, Optional
from .models import Config, NapCatConfig, PluginConfig
from .storage import ConfigStorage
from .security import strong_password_check, generate_strong_token


class ConfigValueError(Exception):
    def __init__(self, msg: str):
        super().__init__(f"配置项错误: {msg}")


class ConfigManager:
    """配置管理器。

    职责：
    - 配置的懒加载、重载、保存
    - 嵌套键的读写（支持 'napcat.ws_uri' 形式）
    - 安全检查与修复
    - 目录创建等副作用操作
    """

    def __init__(self, path: Optional[str] = None):
        self._storage = ConfigStorage(path)
        self._config: Optional[Config] = None

    @property
    def config(self) -> Config:
        if self._config is None:
            self._config = self._storage.load()
        return self._config

    def reload(self) -> Config:
        self._config = self._storage.load()
        return self._config

    def save(self) -> None:
        if self._config is not None:
            self._storage.save(self._config)

    # ---- 通用读写 ----

    def update_value(self, key: str, value) -> None:
        """更新配置项。支持直接键 ('debug') 和嵌套键 ('napcat.ws_uri')。"""
        full_key = self.config.get_field_paths().get(key, key)
        parts = full_key.split(".")
        obj = self.config
        for part in parts[:-1]:
            if not hasattr(obj, part):
                raise ConfigValueError(f"未知的配置项: {key}")
            obj = getattr(obj, part)
        final = parts[-1]
        if not hasattr(obj, final):
            raise ConfigValueError(f"未知的配置项: {key}")
        setattr(obj, final, value)

    # ---- 子配置访问 ----

    @property
    def napcat(self) -> NapCatConfig:
        return self.config.napcat

    @property
    def plugin(self) -> PluginConfig:
        return self.config.plugin

    @property
    def bot_uin(self) -> str:
        return self.config.bot_uin

    @property
    def root(self) -> str:
        return self.config.root

    @property
    def debug(self) -> bool:
        return self.config.debug

    @debug.setter
    def debug(self, value: bool) -> None:
        self.config.debug = value

    # ---- 便捷方法 ----

    def get_uri_with_token(self) -> str:
        return f"{self.napcat.ws_uri}?access_token={self.napcat.ws_token}"

    def is_local(self) -> bool:
        return self.napcat.ws_host in ("localhost", "127.0.0.1")

    def is_default_uin(self) -> bool:
        from .models import DEFAULT_BOT_UIN
        return self.config.bot_uin == DEFAULT_BOT_UIN

    def is_default_root(self) -> bool:
        from .models import DEFAULT_ROOT
        return self.config.root == DEFAULT_ROOT

    # ---- 安全检查 ----

    def get_security_issues(self, auto_fix: bool = True) -> List[str]:
        """检查安全性问题。auto_fix=True 时自动修复弱 token。"""
        issues = []

        if self.napcat.ws_listen_ip == "0.0.0.0":
            if not strong_password_check(self.napcat.ws_token):
                if auto_fix:
                    self.napcat.ws_token = generate_strong_token()
                else:
                    issues.append("WS 令牌强度不足")

        if self.napcat.enable_webui:
            if not strong_password_check(self.napcat.webui_token):
                if auto_fix:
                    self.napcat.webui_token = generate_strong_token()
                else:
                    issues.append("WebUI 令牌强度不足")

        return issues

    def get_issues(self) -> List[str]:
        """返回所有配置问题（安全 + 必填项）。"""
        issues = self.get_security_issues()
        if self.is_default_uin():
            issues.append("机器人 QQ 号未配置")
        if self.is_default_root():
            issues.append("管理员 QQ 号未配置")
        return issues

    def ensure_plugins_dir(self) -> None:
        """确保插件目录存在。"""
        d = self.plugin.plugins_dir
        if not os.path.exists(d):
            os.makedirs(d)


# ---- 单例 ----

_default_manager: Optional[ConfigManager] = None


def get_config_manager(path: Optional[str] = None) -> ConfigManager:
    """获取配置管理器单例。"""
    global _default_manager
    if _default_manager is None or path is not None:
        _default_manager = ConfigManager(path)
    return _default_manager
```

**与 legacy `manager.py` 的关键差异**：

| 对比项 | Legacy | 新设计 |
|--------|--------|--------|
| `set_ws_uri` / `set_ws_token` 等 | 逐字段暴露 setter | **移除**，用 `update_value("napcat.ws_uri", val)` 或直接 `manager.napcat.ws_uri = val` |
| `fix_invalid_config_interactive()` | manager 内含 `input()` 交互 | **移除**，交互逻辑属于 CLI 层 |
| `UinValueError` / `ensure_uin` | 在 manager 模块 | **移除**，uin 校验由 pydantic validator 处理 |
| 安全逻辑 | 散落在 `NapCatConfig.get_security_issues()` 和 manager | 统一在 `ConfigManager.get_security_issues()` |

#### 3.2.5 `config/__init__.py` — 导出

```python
from .manager import ConfigManager, get_config_manager, ConfigValueError
from .models import Config, NapCatConfig, PluginConfig, BaseConfig
from .storage import ConfigStorage, CONFIG_PATH
from .security import strong_password_check, generate_strong_token

__all__ = [
    "ConfigManager",
    "get_config_manager",
    "ConfigValueError",
    "Config",
    "NapCatConfig",
    "PluginConfig",
    "BaseConfig",
    "ConfigStorage",
    "CONFIG_PATH",
    "strong_password_check",
    "generate_strong_token",
]
```

### 3.3 配置模块依赖关系

```
config/__init__.py
├── models.py       ← 依赖 pydantic（纯数据，零业务逻辑）
├── storage.py      ← 依赖 models, pyyaml
├── security.py     ← 零外部依赖
└── manager.py      ← 依赖 models, storage, security
```

---

## 4. Legacy 对比总结

### 4.1 日志模块

```
Legacy                              →    New
────────────────────────────────────────────────────────────
logger.py (单文件, ~350行)          →    logger/ (包, 5个文件)
  FoldedLogger(logging.Logger)      →      BoundLogger (组合模式)
    ├─ reset() 截断                 →      MessageFoldFilter
    ├─ debug/info/error/warning     →      统一 _log() 方法
    └─ stacklevel 手动管理           →      _log() 自动处理
  ColoredFormatter                  →      ColoredFormatter (独立文件)
  RegisteredLoggerFilter            →      移除（标准级别管理替代）
  setup_logging() 导入时执行        →      setup_logging() 显式调用, 幂等
  get_log() → FoldedLogger          →      get_log() → BoundLogger + setLevel(DEBUG)
  Status.register_logger()          →      移除（不再需要注册表）
```

### 4.2 配置模块

```
Legacy                              →    New
────────────────────────────────────────────────────────────
config/models.py                    →    config/models.py (纯数据)
  NapCatConfig.get_issues()         →      移至 ConfigManager
  NapCatConfig.fix_security_issues()→      移至 ConfigManager
  PluginConfig.ensure_dir_exists()  →      移至 ConfigManager
  Config.is_local()                 →      移至 ConfigManager
config/utils.py                     →    config/security.py (更名)
config/storage.py                   →    config/storage.py (不变)
config/manager.py                   →    config/manager.py (精简)
  set_ws_uri/set_ws_token/...       →      移除, 直接属性访问
  fix_invalid_config_interactive()  →      移除, 交互归 CLI
  UinValueError/ensure_uin          →      移除, pydantic 处理
```

---

## 5. 移除的 legacy 功能

| 功能 | 原位置 | 移除原因 |
|------|--------|----------|
| `Status.register_logger()` | `status.py` | 不再需要注册表，改用标准级别管理（见 2.1.1） |
| `Status.is_registered_logger()` | `status.py` | 同上 |
| `Status.get_registered_loggers()` | `status.py` | 同上 |
| `Status.update_logger_level()` | `status.py` | 应在 `setup_logging` 或配置变更回调中处理 |
| `RegisteredLoggerFilter` | `logger.py` | 移除，标准级别管理替代（见 2.1.1） |
| `fix_invalid_config_interactive()` | `manager.py` | 交互式 I/O 不属于配置管理器职责 |
| `ncatbot_config = get_config_manager()` 在 `__init__` | `config/__init__.py` | 避免模块导入的全局副作用 |
| `setup_logging()` 模块级自动执行 | `logger.py` 尾部 | 显式初始化 |

---

## 6. 迁移步骤

1. **创建 `ncatbot/utils/logger/` 包**，实现 `core.py` / `formatter.py` / `filters.py` / `setup.py` / `tqdm.py`
2. **创建 `ncatbot/utils/config/` 包**，实现 `models.py` / `storage.py` / `security.py` / `manager.py`
3. **更新 `ncatbot/utils/__init__.py`**，移除 `__path__` 代理机制，直接从新模块导入
4. **全局搜索替换** `from ncatbot.utils.logger import get_log` —— 接口不变，无需修改
5. **更新 `Status` 类**，移除 `register_logger` / `is_registered_logger` / `update_logger_level`
6. **在应用入口** (`BotClient.__init__` 或 `main.py`) 添加 `setup_logging()` 显式调用
